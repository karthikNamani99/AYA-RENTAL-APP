<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable name="rowIcon" type="android.graphics.drawable.Drawable"/>
        <variable name="rowText" type="java.lang.String"/>
        <variable name="rowTextColor" type="java.lang.Integer"/>
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:clickable="true"
        android:focusable="true"
        android:foreground="?attr/selectableItemBackground"
        android:paddingStart="6dp"
        android:paddingEnd="6dp">

        <ImageView
            android:layout_width="22dp"
            android:layout_height="22dp"
            android:src='@{rowIcon}'
        app:tint="#5A5A5A"/>

        <TextView
            android:id="@+id/tvRow"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="12dp"
            android:text='@{rowText}'
            android:textSize="14sp"
            android:textColor='@{rowTextColor != null ? rowTextColor : @color/black}'/>

        <ImageView
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src='@drawable/ic_chevron_right_24'
            app:tint="#9E9E9E"/>
    </LinearLayout>
</layout>
